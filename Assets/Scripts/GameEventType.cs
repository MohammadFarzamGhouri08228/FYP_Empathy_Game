using UnityEngine;

/// <summary>
/// Enumeration of different game events that can trigger dialogue
/// </summary>
public enum GameEventType
{
    BombEncountered,
    FireEncountered,
    SpikeEncountered,
    ObstacleEncountered,
    Checkpoint1Reached,
    Checkpoint2Reached,
    Checkpoint3Reached,
    Checkpoint4Reached,
    Checkpoint5Reached,
    // Add more checkpoint events as needed
}
