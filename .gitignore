# This .gitignore file is for a Unity Project.
# It should be placed at the root of your project directory.

# Build results
/Build/
/Builds/

# Unity generated directories and files
# The 'Library' folder is local cache data that can be regenerated by Unity.
/Library/
# The 'Temp' folder contains temporary build and processing files.
/Temp/
# The 'Obj' folder is used by the compiler.
/Obj/
# The 'Logs' folder contains Unity editor and player logs.
/Logs/
# The 'UserSettings' folder contains local user-specific settings.
/UserSettings/

# Visual Studio / Rider generated files
# VS/Rider solution and project files
*.csproj
*.unityproj
*.sln
*.suo
*.sln.DotSettings
*.user
*.userprefs

# macOS specific files
.DS_Store
# Temporary files created by macOS applications
.TemporaryItems

# Other common temporary files
~*
thumbs.db

# Optional: Uncomment if you want to ignore all Asset serialization files.
# Most users should keep these, as they contain important metadata.
#*.meta

# Ignore specific files often modified locally
# Player settings and build information
/ProjectSettings/ProjectSettings.asset
/ProjectSettings/PackageManagerSettings.asset
/ProjectSettings/Physics2DSettings.asset

# Local input settings (might be better to track these, but sometimes ignored)
#/Assets/InputSystem_Actions.inputactions

# Local Volume Profile (often user-specific lighting/post-processing setup)
#/Assets/DefaultVolumeProfile.asset
#/Assets/DefaultVolumeProfile.asset.meta

# Universal Render Pipeline (URP) settings - often shared, but could be ignored if locally customized
#/Assets/UniversalRenderPipelineGlobalSettings.asset
#/Assets/UniversalRenderPipelineGlobalSettings.asset.meta

# ----------------------------------------------------------------------
# IMPORTANT:
# ----------------------------------------------------------------------
# You should **always** commit your 'Assets' and 'ProjectSettings' folders (except for the specific files listed above if you choose to ignore them),
# as they contain all the necessary data for your project to run.